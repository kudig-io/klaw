apiVersion: v1
kind: Service
metadata:
  name: {{ include "klaw.fullname" . }}
  labels:
    {{- include "klaw.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "klaw.selectorLabels" . | nindent 4 }}
