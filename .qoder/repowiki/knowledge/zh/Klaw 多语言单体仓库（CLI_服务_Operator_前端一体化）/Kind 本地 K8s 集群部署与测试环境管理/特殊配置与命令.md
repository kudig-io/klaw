前置依赖 Docker、Kind、kubectl（macOS 可通过 brew 安装）。标准流程：`cd deployment/kind && ./manage.sh check` → `./manage.sh create` → `./manage.sh deploy-sample` → `./manage.sh status`；导出独立 kubeconfig 后使用：`./manage.sh export-config && export KUBECONFIG=$(pwd)/kubeconfig`。