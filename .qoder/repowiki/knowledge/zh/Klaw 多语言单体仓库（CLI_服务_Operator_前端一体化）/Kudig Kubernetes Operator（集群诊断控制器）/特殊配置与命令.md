本地运行：`go mod tidy && go run cmd/main.go`（需要 kubeconfig）；构建镜像：`docker build -t kudig/kudig-operator:latest .`；Helm 安装：`helm install kudig-operator ./helm/kudig-operator -n kudig-system --create-namespace`；CRD 可通过 `kubectl apply -f config/crd/` 或 Helm chart 中的 crds.yaml 安装。