kind 本地开发流程：先 `docker build -t kudig-io/klaw:dev .` 构建镜像，再用 `kind load docker-image` 注入节点，最后 `helm upgrade --install klaw helm/klaw -f helm/klaw/values-kind.yaml -n klaw --create-namespace` 安装。生产部署需通过 `secrets.apiToken` 或 `existingSecret` 提供 API Token，并按需启用钉钉/飞书 webhook 凭据。