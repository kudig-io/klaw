- 每个业务域以 Manager 结构体封装核心逻辑，对外暴露方法供 API handler 调用
- 需要持久化的数据统一通过 storage.Store 的 Put/Get/Delete 操作存取，避免直接操作数据库
- 可插拔组件（消息通道、分析器、报告器）均定义 interface 并通过 registry 或 NewXxx 工厂函数注册
- 测试文件与被测文件同名并以 _test.go 后缀存放于同一目录
- Kubernetes 相关操作统一通过 kubernetes.Manager 抽象，避免在业务模块中直接依赖 client-go