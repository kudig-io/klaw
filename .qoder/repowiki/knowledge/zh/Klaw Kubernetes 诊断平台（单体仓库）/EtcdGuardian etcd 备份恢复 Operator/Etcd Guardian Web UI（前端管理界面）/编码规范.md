- 页面与功能模块以独立 `.tsx` 文件组织在 `src/components/` 下，并通过 `index.ts` 集中 re-export 供 `App.tsx` 按需引入。
- 视图内导航采用基于字符串 `activeTab` 的 switch-case 渲染模式，而非基于路由的状态管理。
- 组件内部状态优先使用 React Hooks（如 `useState`）维护本地 mock 数据，尚未接入远端 API。
- 样式全部通过 Tailwind CSS 原子类编写，深色主题以 `bg-gray-900`、`text-white`、`bg-blue-600` 等类组合实现。