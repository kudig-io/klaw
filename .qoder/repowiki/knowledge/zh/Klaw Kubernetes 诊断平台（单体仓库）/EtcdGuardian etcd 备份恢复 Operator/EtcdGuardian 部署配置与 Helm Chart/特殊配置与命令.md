可通过 `helm install etcdguardian ./charts/etcdguardian -n etcd-guardian-system --create-namespace` 部署；也可将 `config/rbac/*`、`config/manager/*`、`config/security/*` 依次 `kubectl apply -f` 到 `etcd-guardian-system` 命名空间进行裸部署。