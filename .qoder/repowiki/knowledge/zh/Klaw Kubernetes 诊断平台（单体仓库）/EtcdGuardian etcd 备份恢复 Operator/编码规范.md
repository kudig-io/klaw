- CRD 类型定义集中在 `api/v1alpha1/`，并通过 controller-gen 自动生成 deepcopy 代码。
- 可观测性统一通过 `pkg/metrics` 暴露指标，并由 Prometheus/Grafana 采集与展示。
- 所有对外服务（Operator、Backend、Web UI）均通过独立的 Dockerfile 构建镜像，并在 charts/config 中以 Deployment 形式部署。
- 安全与网络隔离通过 `config/security` 下的 NetworkPolicy、TLS Secret 与 RBAC 清单集中声明。