运行前需设置 `KUDIG_AI_API_KEY`（必需），可选 `KUDIG_AI_PROVIDER`(openai|qwen|ollama)、`KUDIG_AI_MODEL`、`KUDIG_AI_BASE_URL`、`KUDIG_AI_TIMEOUT`、`KUDIG_AI_LANGUAGE`、`KUDIG_AI_MAX_TOKENS`、`KUDIG_AI_TEMPERATURE`；单元测试通过 `go test` 直接执行，无需外部服务。