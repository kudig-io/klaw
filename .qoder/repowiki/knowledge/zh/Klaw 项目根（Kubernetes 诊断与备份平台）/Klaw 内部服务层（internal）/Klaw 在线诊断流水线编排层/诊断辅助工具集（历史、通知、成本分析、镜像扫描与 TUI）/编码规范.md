- 每个子包提供 `*_test.go` 配套测试文件，测试命名与源文件一一对应。
- 对外暴露的构造函数统一采用 `NewXxx(...)` 模式，并在内部封装错误信息使用 `%w` 包装 error。
- 结构化数据通过 `json:` tag 序列化，格式化输出函数统一命名为 `FormatResult` 并使用 `strings.Builder` 拼接。
- 可插拔组件通过接口抽象（如 `Notifier`），具体实现以独立结构体提供 `Name()` 和 `Send()` 方法。
- 耗时或外部依赖操作通过 `context.Context` 传递，支持超时与取消。