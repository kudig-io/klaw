镜像扫描需要系统已安装 `trivy` 且可在 PATH 中找到；通知渠道通过环境变量 `KUDIG_SLACK_WEBHOOK_URL`、`KUDIG_DINGTALK_WEBHOOK_URL`、`KUDIG_WECHAT_WEBHOOK_URL`、`KUDIG_NOTIFY_MIN_SEVERITY` 配置；历史记录默认写入用户家目录 `~/.klaw/history/`，权限为 0750/0600。