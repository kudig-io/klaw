`make dev` 并行启动前端 dev server 与 Go 后端；`make build` 先 `npm ci && npm run build` 再 `go build ./cmd/klaw`；`make docker-build` 执行三阶段 Docker 构建；`make helm-install|upgrade|uninstall` 管理 Klaw Helm Chart；`make test` 同时运行 Go 与前端测试。