- 所有 Go 代码通过单一 `go.mod` 管理，子模块以 package 形式组织而非独立 module
- 构建流程统一由 Makefile 驱动，前端与后端构建目标分离但由 `build`/`dev` 聚合调用
- Docker 镜像采用多阶段构建，前端与后端分别构建后合并到最小化 alpine 运行时镜像
- 配置通过 `configs/config.yaml` 注入，二进制通过 `-ldflags` 裁剪体积并禁用 CGO