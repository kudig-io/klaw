- 错误统一通过 `fmt.Errorf("action: %w", err)` 包装底层错误，保持错误链可追溯。
- HTTP 响应统一检查状态码并返回 `unexpected status` 错误，解码失败直接透传。
- 所有公开方法接受 `context.Context` 作为第一个参数，支持取消与超时控制。
- 共享数据结构集中放在 `types.go`，并通过 JSON 标签暴露字段名，避免分散定义。
- Manager 的写操作（Create/Delete）后调用 `invalidateCache()` 使缓存失效，读操作优先命中 30 秒内缓存。