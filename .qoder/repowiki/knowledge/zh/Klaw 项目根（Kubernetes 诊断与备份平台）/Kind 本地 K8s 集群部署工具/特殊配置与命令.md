前置依赖：Docker、Kind、kubectl（macOS 可通过 brew 安装）。常用流程：`cd deployment/kind && ./manage.sh check` → `./manage.sh create` → `./manage.sh deploy-sample` → `./manage.sh status`；导出独立 kubeconfig：`./manage.sh export-config && export KUBECONFIG=$(pwd)/kubeconfig`。