- 所有对外输出通过 `print_info/print_success/print_warning/print_error` 四个彩色函数统一格式化，避免直接 echo。
- 每个命令子函数在执行前按需调用 `check_docker`、`check_kind`、`check_kubectl` 进行依赖校验，失败即 `exit 1`。
- 集群名称集中定义为变量 `CLUSTER_NAME="klaw-test"`，并通过 `${SCRIPT_DIR}` 动态解析配置文件与数据目录路径。
- kubectl 操作统一使用 `--dry-run=client -o yaml | kubectl apply -f -` 模式生成并应用资源，避免编写完整 YAML 文件。